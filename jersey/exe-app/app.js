console.log();

const one = Buffer.from('Hel');
const ote = Buffer.from('rld');

const b = Buffer.from('lo');

const tr = Buffer.from('Hello World !!!')

console.log(tr.toString());


console.log(tr.toJSON(tr));